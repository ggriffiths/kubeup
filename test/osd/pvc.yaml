kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: no-auth-mysql-data
  annotations:
    volume.beta.kubernetes.io/storage-class: osd-sc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
